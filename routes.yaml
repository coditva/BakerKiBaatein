routes:
  /login/: /#/portal/signup

collections:
  /:
    permalink: /{slug}/
    template: index
    filter: tag:-newsletter
  /newsletter/:
    permalink: /newsletter/{slug}/
    template: newsletter
    filter: tag:newsletter

taxonomies:
  tag: /tag/{slug}/
  author: /author/{slug}/
